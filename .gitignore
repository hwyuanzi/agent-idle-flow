# Dependencies
node_modules/

# Compiled output — rebuild with `npm run build`
dist/

# Claude Code workspace settings (tool permissions, local config)
.claude/

# macOS metadata
.DS_Store
.DS_Store?
._*

# SQLite databases (tasks live in ~/.aiflow/tasks.db, not the repo)
*.db
*.sqlite
*.sqlite3

# Environment / secrets (none used, but good hygiene)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
